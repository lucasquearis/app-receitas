import React from 'react';
import {
  Footer,
  Header,
} from '../components';

const Profile = () => (
  <div>
    <Header page="Perfil" />
    Perfil
    <Footer />
  </div>
);

export default Profile;
