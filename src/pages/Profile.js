import React from 'react';
import {
  Footer,
  Header,
  UserProfile,
} from '../components';

const Profile = () => (
  <div>
    <Header page="Perfil" />
    <UserProfile />
    <Footer />
  </div>
);

export default Profile;
