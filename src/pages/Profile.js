import React from 'react';
import {
  Footer,
  Header,
  UserProfile,
} from '../components';

const Profile = () => (
  <>
    <Header page="Perfil" />
    <UserProfile />
    <Footer />
  </>
);

export default Profile;
