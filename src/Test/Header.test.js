// import React from 'react';
// import { screen } from '@testing-library/react';
// import Header from '../Component/Header';
// import renderWithRouter from './renderWithRouter';

/* describe('Testando o componente Header', () => {
  beforeEach(() => {
//    renderWithRouter(<Header />);
  });
  test('Testa se a página contém o ícone profile:', () => {
    const imgAbout = screen.getByRole('img');
    expect(imgAbout).toHaveAttribute('src', 'src/images/profileIcon.svg');
  });
  test('Testa se a página contém o ícone de pesquisa:', () => {
    const imgAbout = screen.getByRole('img');
    expect(imgAbout).toHaveAttribute('src', 'src/images/searchIcon.svg');
  });
}); */
