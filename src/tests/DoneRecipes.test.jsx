import React from 'react';
// import { fireEvent, screen } from '@testing-library/react';
import App from '../App';
import renderWithRouter from './renderWithRouter';

describe('Testa tela de receitas feitas', () => {
  beforeEach(() => {
    renderWithRouter(<App />);
  });
  it('Existência dos botões de filtragem', () => {

  });

  it('Existência dos elementos de descrição da receita feita', () => {

  });

  it('Botão de compartilhar', () => {

  });

  it('Funcionamento dos filtros', () => {

  });

  it('Redirecionamento para tela de detalhes', () => {

  });
});
