const removeSomeSpaceAndSlash = (strID) => (strID.replace(' ', '')
  .replace(' ', '').replace(' ', '').replace(' ', '')
  .replace('/', '')
  .replace('/', ''));

export default removeSomeSpaceAndSlash;
